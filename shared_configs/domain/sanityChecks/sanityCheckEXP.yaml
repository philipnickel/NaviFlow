boundaries:
  left:
    velocity: {bc: dirichlet, value: ["np.exp(x[0])*np.sin(x[1])+x[0]", "np.cos(x[1])+x[0]+0.1"]}
    pressure: {bc: neumann, gradient: 0.0}
  right:
    velocity: {bc: dirichlet, value: ["np.exp(x[0])*np.sin(x[1])+x[0]", "np.cos(x[1])+x[0]+0.1"]}
    pressure: {bc: neumann, gradient: 0.0}
  top:
    velocity: {bc: dirichlet, value: ["np.exp(x[0])*np.sin(x[1])+x[0]", "np.cos(x[1])+x[0]+0.1"]}
    pressure: {bc: neumann, gradient: 0.0}
  bottom:
    velocity: {bc: dirichlet, value: ["np.exp(x[0])*np.sin(x[1])+x[0]", "np.cos(x[1])+x[0]+0.1"]}
    pressure: {bc: neumann, gradient: 0.0}