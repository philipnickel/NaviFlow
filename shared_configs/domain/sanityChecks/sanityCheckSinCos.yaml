boundaries:
  left:
    velocity: {bc: wall, value: ["np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])", "np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
  right:
    velocity: {bc: wall, value: ["np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])", "np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
  top:
    velocity: {bc: wall, value: ["np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])", "np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
  bottom:
    velocity: {bc: wall, value: ["np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])", "np.sin(np.pi*(x[0]+x[1]))+np.cos(np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
