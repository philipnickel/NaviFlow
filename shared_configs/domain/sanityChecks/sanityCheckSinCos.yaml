boundaries:
  left:
    velocity: {bc: dirichlet, value: ["np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])", "np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
  right:
    velocity: {bc: dirichlet, value: ["np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])", "np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
  top:
    velocity: {bc: dirichlet, value: ["np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])", "np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
  bottom:
    velocity: {bc: dirichlet, value: ["np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])", "np.sin(4*np.pi*(x[0]+x[1]))+np.cos(4*np.pi*x[0]*x[1])"]}
    pressure: {bc: neumann, gradient: 0.0}
